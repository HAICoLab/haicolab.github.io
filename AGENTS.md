# 저장소 작업 규칙

## CNAME 보존

- 이 사이트는 GitHub Pages에서 `main` 브랜치의 루트를 배포하며, 사용자 지정 도메인은 `haicolab.sogang.ac.kr`이다.
- 루트의 `CNAME`은 도메인 연결에 필요한 운영 설정이다. 파일명은 대문자 `CNAME`으로 유지하고, 내용에는 `haicolab.sogang.ac.kr` 한 줄만 둔다. `https://`, 경로, 다른 도메인을 추가하지 않는다.
- 사용자의 명시적인 도메인 변경 요청이 없다면 `CNAME`을 삭제하거나 이름·위치·내용을 변경하지 않는다. `.gitignore`에 추가하거나 Git 추적에서 제외하지 않는다.
- 사이트 전체 교체, 빌드 결과 복사, 파일 정리, 이전 버전 복원 시에도 배포할 루트에 `CNAME`을 보존한다. 다른 템플릿이나 저장소의 파일로 덮어쓰지 않는다.
- 커밋·푸시 전에 배포할 변경 사항에 `CNAME`의 삭제나 변경이 없는지 확인하고, 파일이 Git에 포함되어 있으며 도메인 값이 정확한지 확인한다. 배포 후에는 `https://haicolab.sogang.ac.kr/`에서 실제 홈페이지가 열리는지 확인한다.
- 일반적인 홈페이지 콘텐츠 수정에는 GitHub Pages의 Custom domain 설정이나 조직의 Verified domain 설정을 제거하지 않는다.
